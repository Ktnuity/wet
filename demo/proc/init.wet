proc fib int in int do
    dup 1 > if
        dup -- fib
        swap 2 - fib
        +
    end
end

proc try_fib in int do
    dup tostring ": " + swap fib + "\n" + puts
#dup tostring ": " + swap "\n" + puts
end

"fib\n" puts

0 while dup 15 < do
    dup try_fib
    ++
end
drop

"fib done\n" puts

